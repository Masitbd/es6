/* const findSquare = (number) => number * number;
const result = findSquare(9);
console.log(result); */

//const numbers = [23, 81, 49, 33, 2];

/* numbers.map((number) => console.log(parseInt(number / 7))); */

/* numbers.forEach((number) => console.log(number)); */

//let result = numbers.find((number) => number > 71);

//console.log(result);

/* [a, b, ...rest] = numbers;
console.log(...rest); */

const students = {
  name: "Murad",
  section: "Bsc",
  fee: 50000,
};

const { name, section, fee } = students;

console.log(section)
